# Staging Environment Configuration
base.url=https://
timeout.seconds=15
screenshot.enabled=true
browser.headless=false
retry.count=3

# Environment-specific settings
environment.name=Staging
log.level=INFO
test.data.validation=moderate